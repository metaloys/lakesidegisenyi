/**
 * @deprecated Admin login uses Supabase Auth (auth.users), not this file.
 * Credentials are created in Supabase Dashboard → Authentication → Users.
 * See lakeside_schema.sql (footer) to link the user to Lakeside in public.profiles.
 */
